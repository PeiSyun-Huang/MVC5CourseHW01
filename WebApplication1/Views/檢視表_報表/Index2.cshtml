@model IEnumerable<WebApplication1.Models.檢視表_報表>

@{
    ViewBag.Title = "Index2";
}

<h2>Index2</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.聯絡人數量)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.銀行帳戶數量)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.聯絡人數量)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.銀行帳戶數量)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.客戶名稱 }) |
            @Html.ActionLink("Details", "Details", new { id=item.客戶名稱 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.客戶名稱 })
        </td>
    </tr>
}

</table>
